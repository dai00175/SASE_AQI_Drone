Place your TLS certificate pair here before starting the `nginx` service.

Required filenames:

- `fullchain.pem`
- `privkey.pem`

For local validation, you can generate a self-signed certificate or mount a Let's Encrypt directory into this path.
